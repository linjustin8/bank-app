from sqlalchemy.orm import declarative_base

Base = declarative_base()
#the base class is used to create the db tables and models for the app. It is a base class that is inherited by all the other models in the application